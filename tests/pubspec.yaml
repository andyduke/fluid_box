name: fluid_kit_tests
description: FluidKit tests
publish_to: none
version: 1.0.0
author:
homepage:
repository:
issue:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  fluid_kit:
    path: ../package

dev_dependencies:
  flutter_test:
    sdk: flutter
  golden_toolkit: ^0.15.0

flutter:
